#Напишите функцию greet, которая принимает имя пользователя в качестве аргумента и выводит приветствие с этим именем.

# 1 строка используем def для начала определения функции называем функцию greet и задаем ей аргумент name
# 2 строка используем F стоку для того что бы поместить аргумент для конечного ответа
# 4 строка вызов функции
# def greet (name):
#     return (f"Привет {name} ʕ•ᴥ•ʔ")
#
# print(greet("Vanya"))


#Создайте функцию square, которая возвращает квадрат переданного ей числа.

# 1 строка используем def для начала определения функции называем функцию square и задаем ей аргумент number
# 2 строка выводим значение аргумета в квадрате
# 4 строка вызов функции
# def square (number):
#     print((number)** 2)
# square(87)

#Реализуйте функцию max_of_two, которая принимает два числа в качестве аргументов и возвращает большее из них.

# 1 строка используем def для начала определения функции называем функцию max_of_two и задаем ей аргументы M и C
# 2 строка идет сравнение аргументов
# 4 строка если не выполнилось 1 аргумент то выведиться 2 аргумент

# def max_of_two (M,C):
#     if M > C:
#         print(M)
#     elif C > M:
#         print(C)
#     elif C == M:
#         print("Аргументы равны")
#
# max_of_two(7,6)

#Напишите функцию describe_person, принимающую имя и возраст человека, и печатающую эту информацию в читаемом виде. Сделайте возраст опциональным аргументом со значением по умолчанию 30.

# 1 строка используем def для начала определения функции называем функцию describe_person и задаем ей аргументы name и age ( по умолчанию = 30 )
# 2 строка используем F строку для того что бы поместить вводимые аргуманты для ответа
# 4 строка вызов функции
# def describe_person (name, age = 30 ):
#     print(F"Вы: {name} , Ваш возраст: {age} лет/года")
#
# describe_person("Vanya", 67)

#Напишите функцию is_prime, которая определяет, является ли число простым, и возвращает True или False соответственно.

# 1 строка используем def для начала определения функции называем функцию is_prime и задаем ей аргумент x
# 2 строка далее проверям аргумент на остаток от деления
# 4 строка так же проверка аргумента на остаток от деления
# 7 строка вызов функции

def is_prime (x):
    if x % 2 == 0 or x % 3 == 0 or x % 5 == 0 or x % 7 == 0:
        print(F"Число {x} составное ")
    elif x % 2 == 1 and x % x == 0:
        print(F"Число {x} простое ")

is_prime(17)
